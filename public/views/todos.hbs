<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todos</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1 class="text-success">Your Todos</h1>
      <a href="/logout" class="btn btn-danger">Logout</a>
    </div>
    <ul class="list-group mt-4">
      {{#each todos}}
      <li class="list-group-item bg-dark text-success">
        <h5>{{this.title}}</h5>
        <p>{{this.description}}</p>
        <p>Status: {{#if this.isFinished}}Finished{{else}}Not Finished{{/if}}</p>
        <a href="/todo/{{this.id}}/status" class="btn custom-file-upload me-2">
          {{#if this.isFinished}}Mark as Unfinished{{else}}Mark as Finished{{/if}}
        </a>
        <a href="/todo/{{this.id}}/edit" class="btn btn-warning me-2">Edit</a>
        <a href="/todo/{{this.id}}/delete" class="btn btn-danger">Delete</a>
      </li>
      {{/each}}
    </ul>
    <a href="/todo/create_todo" class="btn custom-file-upload mt-3">Create New Todo</a>
  </div>
</body>
</html>
